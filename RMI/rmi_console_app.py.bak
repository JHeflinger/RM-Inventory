from rmi import sysPrint, usrPrint, handleInput
appRunning = True
rmi.sysPrint("Welcome to RMI (Console app)! Enter a command to get started!")
while appRunning:
    rmi.usrPrint("")
    appRunning = rmi.handleInput(input())